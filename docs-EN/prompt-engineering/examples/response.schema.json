{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/ai-agent-engineer/ticket-routing-response.schema.json",
  "x-contract-version": "ticket-routing-response-1.0.0",
  "title": "Ticket routing response",
  "description": "Offline teaching contract; runtime verifies this subset matches its hand-written validator, then applies business semantics.",
  "type": "object",
  "properties": {
    "label": {
      "type": "string",
      "enum": ["billing", "technical", "other"]
    },
    "reason": {
      "type": "string",
      "minLength": 1,
      "maxLength": 160
    },
    "evidence": {
      "anyOf": [
        {
          "type": "string",
          "minLength": 1,
          "maxLength": 120
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "required": ["label", "reason", "evidence"],
  "additionalProperties": false
}

