---
title: LLM API 集成学习目录
tags:
  - ai-agent-engineer
  - llm-api
  - learning-path
aliases:
  - LLM API集成目录
  - 大模型 API 集成
source_checked: 2026-07-14
source_baseline:
  - OpenAI API guides and official openai-python README
  - Anthropic Errors, Rate limits, and Streaming Messages documentation
  - Google Gemini API troubleshooting and Rate limits documentation
  - RFC 9110 HTTP Semantics and WHATWG Server-sent events
ai_learning_stage: 3. LLM 应用基础
ai_learning_order: 21
---

# LLM API 集成

> 资料核验日期：2026-07-14。端点、SDK 方法、默认重试、模型标识、事件类型、速率限制和价格均可能变化；本库教授稳定的集成边界，实际接入必须以所选供应商当前官方文档、SDK 版本和 API 规范为准。

## 知识库简介

LLM API 集成把提示、上下文与工具调用接入可运行软件。入门示例往往只有一次请求，但生产系统还需处理身份认证、分层超时、单一重试预算、限流、结构化输出、流中失败、用量、日志、隐私与供应商差异。本库先用供应商无关的离线 client contract 学习这些责任，再指导如何增加真实 adapter；canonical 事件名只是本库合同，不冒充任何供应商的原始协议。

## 在总路线中的位置

本库是 LLM 应用基础的工程出口。[[提示词工程/00-目录|提示词工程]]提供任务与输出合同，[[上下文工程/00-目录|上下文工程]]提供证据与状态；本库负责可靠传输、解析、观测和错误控制。后续 Tool Calling、RAG、Agent 框架和 LLMOps 都依赖这些基础。

## 学习目标

- 在 Windows 11 / PowerShell 7 中用 `venv + pip` 建立最小 Python 环境，并安全读取密钥。
- 理解 HTTP、官方 SDK 与应用 adapter 的职责边界。
- 构造版本可追溯的消息、模型配置和请求元数据。
- 正确处理结构化输出、SSE 流式事件、拒绝、截断与中途错误。
- 为超时、429、连接与 5xx 等暂时故障设计有上限、带抖动的重试；不重试永久错误。
- 记录请求 ID、提示版本、用量、延迟与结果状态，同时保护敏感内容。
- 用稳定 `operation_id` 关联同一业务操作的 attempts，并把模型生成与外部副作用分离。
- 通过 capability check 显式拒绝无法等价映射的供应商功能。

## 前置知识

- Python 函数、类、异常与 JSON 基础。
- HTTP 请求/响应、状态码和环境变量的基本概念。
- 已完成提示词与上下文工程更佳，但可边学边补。

## 推荐学习顺序

1. [[LLM API集成/01-环境、依赖与密钥管理|环境、依赖与密钥管理]]：建立可复现且不泄密的本机环境。
2. [[LLM API集成/02-HTTP、SDK与请求生命周期|HTTP、SDK 与请求生命周期]]：理解一次调用真正经过哪些层。
3. [[LLM API集成/03-消息、配置与版本意识|消息、配置与版本意识]]：设计供应商无关的请求合同。
4. [[LLM API集成/04-结构化输出与流式响应|结构化输出与流式响应]]：正确解析完整与增量结果。
5. [[LLM API集成/05-超时、错误、限流与重试|超时、错误、限流与重试]]：只对可恢复错误做受控重试。
6. [[LLM API集成/06-用量、可观测性与供应商适配|用量、可观测性与供应商适配]]：让请求可追踪、可比较、可替换。
7. [[LLM API集成/07-可靠客户端项目与自测|可靠客户端项目与自测]]：运行 mock transport 的单元测试。

## 动手实践与项目入口

完成 [[LLM API集成/07-可靠客户端项目与自测|可靠客户端项目]]。[[LLM API集成/examples/reliable_client.py|reliable_client.py]] 定义严格请求/响应、错误分类、`Retry-After`、指数退避、抖动、attempt/deadline 双上限与 canonical 流状态机；[[LLM API集成/examples/test_reliable_client.py|test_reliable_client.py]] 用 20 项离线测试验证成功与失败路径。

## 掌握标准

- 能解释为什么 SDK 的默认重试与自己再包一层重试可能导致次数成倍增加。
- 能为连接、首字节、整体请求和业务截止时间设置边界，而不是无限等待。
- 能区分可解析 JSON、schema 合规、语义正确和动作已授权。
- 能在流式 HTTP 已返回成功状态后继续处理错误事件与不完整终止。
- 能用稳定的应用接口隔离供应商差异，不假装所有参数语义完全相同。
- 能在不记录原始敏感内容的前提下定位某次请求使用的配置和失败原因。
- 能运行 20 项测试，并证明未知异常、永久错误、流中失败和缺少终止事件都不会被误判为成功。

## 与其他知识库的关系

- [[提示词工程/00-目录|提示词工程]]的模板版本与 schema 应进入请求元数据。
- [[上下文工程/00-目录|上下文工程]]的预算、来源和状态应在发送前验证。
- [[Tool Calling（含 Function Calling）/00-目录|Tool Calling]]需要把不完整流、重试与真实副作用隔开。
- [[LLMOps/00-目录|LLMOps]]管理跨环境发布与成本，[[运行监控/00-目录|运行监控]]处理线上告警；[[AI安全/00-目录|AI 安全]]、[[隐私计算/00-目录|隐私计算]]和 [[AI治理/00-目录|AI 治理]]约束日志、数据驻留、权限与审计。

## 主要参考资料

- [OpenAI：API errors](https://developers.openai.com/api/docs/guides/error-codes)（访问于 2026-07-14）
- [OpenAI：Rate limits](https://developers.openai.com/api/docs/guides/rate-limits)（访问于 2026-07-14）
- [OpenAI：Streaming API responses](https://developers.openai.com/api/docs/guides/streaming-responses)（访问于 2026-07-14）
- [OpenAI：Structured Outputs](https://developers.openai.com/api/docs/guides/structured-outputs)（访问于 2026-07-14）
- [OpenAI：official Python SDK](https://github.com/openai/openai-python)（访问于 2026-07-14）
- [Anthropic：API errors](https://platform.claude.com/docs/en/api/errors)（访问于 2026-07-14）
- [Anthropic：Rate limits](https://platform.claude.com/docs/en/api/rate-limits)（访问于 2026-07-14）
- [Anthropic：Streaming Messages](https://platform.claude.com/docs/en/build-with-claude/streaming)（访问于 2026-07-14）
- [Google：Troubleshooting guide](https://ai.google.dev/gemini-api/docs/troubleshooting)（访问于 2026-07-14）
- [Google：Gemini API rate limits](https://ai.google.dev/gemini-api/docs/rate-limits)（访问于 2026-07-14）
- [RFC 9110：HTTP Semantics](https://www.rfc-editor.org/rfc/rfc9110)
- [WHATWG：Server-sent events](https://html.spec.whatwg.org/multipage/server-sent-events.html)
