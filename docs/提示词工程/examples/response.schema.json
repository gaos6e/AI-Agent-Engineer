{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/ai-agent-engineer/ticket-routing-response.schema.json",
  "title": "Ticket routing response",
  "description": "Offline teaching contract; validate business semantics in code after schema validation.",
  "type": "object",
  "properties": {
    "label": {
      "type": "string",
      "enum": ["billing", "technical", "other"]
    },
    "reason": {
      "type": "string",
      "minLength": 1,
      "maxLength": 160
    },
    "evidence": {
      "anyOf": [
        {
          "type": "string",
          "minLength": 1,
          "maxLength": 120
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "required": ["label", "reason", "evidence"],
  "additionalProperties": false
}
